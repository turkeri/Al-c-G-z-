import { useEffect, useState } from 'react'
import PageContainer from '../components/Layout/PageContainer'
import { getAdminMe, getAdminSettings, updateAdminSetting, can } from '../services/adminService'
const FIELDS=[['maintenance_mode','Bakım modu','checkbox'],['maintenance_message','Bakım mesajı','text'],['minimum_web_version','Minimum web sürümü','text'],['minimum_android_version','Minimum Android sürümü','text'],['free_analysis_limit','Ücretsiz analiz limiti','number'],['support_email','Destek e-postası','email']]
export default function AdminSettingsPage(){const [values,setValues]=useState({});const [permissions,setPermissions]=useState([]);const [message,setMessage]=useState('');const [error,setError]=useState('');useEffect(()=>{Promise.all([getAdminMe(),getAdminSettings()]).then(([me,s])=>{setPermissions(me.permissions);setValues(Object.fromEntries(s.items.map(x=>[x.key,x.value])))}).catch(e=>setError(e.message))},[]);const writable=can(permissions,'settings:write');async function save(key){if(key==='maintenance_mode'&&values[key]==='true'&&!window.confirm('Bakım modunu açmak istiyor musunuz?'))return;try{await updateAdminSetting(key,values[key]??'');setMessage('Ayar kaydedildi.')}catch(e){setError(e.message)}}return <PageContainer><section className="auth-card"><p className="eyebrow">YÖNETİM</p><h1>Ayarlar</h1>{error&&<p className="auth-error" role="alert">{error}</p>}{message&&<p className="auth-success" role="status">{message}</p>}{FIELDS.map(([key,label,type])=><div className="auth-form" key={key}><label htmlFor={key}>{label}</label>{type==='checkbox'?<input id={key} type="checkbox" checked={values[key]==='true'} disabled={!writable} onChange={e=>setValues({...values,[key]:String(e.target.checked)})}/>:<input id={key} type={type} value={values[key]||''} disabled={!writable} onChange={e=>setValues({...values,[key]:e.target.value})}/>} {writable&&<button className="btn btn-secondary" onClick={()=>save(key)}>Kaydet</button>}</div>)}</section></PageContainer>}
