import { PROXY_BASE_URL } from './aiService.js';import{getAccessToken}from'./authService.js'
async function request(path,method='GET',body){const token=await getAccessToken();if(!token)throw Object.assign(Error('Giriş gerekli.'),{code:401});let response;try{response=await fetch(`${PROXY_BASE_URL.replace(/\/$/,'')}${path}`,{method,headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:body===undefined?undefined:JSON.stringify(body)})}catch{throw Object.assign(Error('Ağ bağlantısı kurulamadı.'),{code:'network'})}const data=await response.json().catch(()=>null);if(!response.ok){const messages={401:'Oturumunuzu yenileyin.',403:'Bu işlem için yetkiniz yok.',409:'Kayıt başka bir işlem tarafından değiştirildi.',422:'Katalog doğrulama kuralları sağlanmadı.'};throw Object.assign(Error(messages[response.status]||'İşlem tamamlanamadı.'),{code:response.status,data})}return data}
export const listRevisions=()=>request('/admin/catalog/revisions');export const createRevision=payload=>request('/admin/catalog/revisions','POST',payload);export const getRevision=id=>request(`/admin/catalog/revisions/${encodeURIComponent(id)}`);export const getRevisionSummary=id=>request(`/admin/catalog/revisions/${encodeURIComponent(id)}/summary`);export const getRevisionReview=id=>request(`/admin/catalog/revisions/${encodeURIComponent(id)}/review`);export const validateRevision=id=>request(`/admin/catalog/revisions/${encodeURIComponent(id)}/validate`,'POST');export const publishRevision=id=>request(`/admin/catalog/revisions/${encodeURIComponent(id)}/publish`,'POST');export const archiveRevision=id=>request(`/admin/catalog/revisions/${encodeURIComponent(id)}/archive`,'POST');export const rollbackCatalog=(targetRevisionId,reason)=>request('/admin/catalog/rollback','POST',{targetRevisionId,reason});export const listEntities=(revisionId,entity,{cursor,limit=50}={})=>request(`/admin/catalog/revisions/${encodeURIComponent(revisionId)}/${entity}?limit=${limit}${cursor?`&cursor=${encodeURIComponent(cursor)}`:''}`);export const getEntity=(revisionId,entity,id)=>request(`/admin/catalog/revisions/${encodeURIComponent(revisionId)}/${entity}/${encodeURIComponent(id)}`);export const createEntity=(revisionId,entity,payload)=>request(`/admin/catalog/revisions/${encodeURIComponent(revisionId)}/${entity}`,'POST',payload);export const updateEntity=(revisionId,entity,id,payload)=>request(`/admin/catalog/revisions/${encodeURIComponent(revisionId)}/${entity}/${encodeURIComponent(id)}`,'PUT',payload);export const archiveEntity=(revisionId,entity,id)=>request(`/admin/catalog/revisions/${encodeURIComponent(revisionId)}/${entity}/${encodeURIComponent(id)}/archive`,'POST')
